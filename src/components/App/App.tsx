import { Layout } from "../Layout";
import { Table } from "../Table";
import { Title } from "../Title";

export const App = () => {
  return (
    <Layout>
      <Title />
      <Table />
    </Layout>
  );
};
